apiVersion: v1
kind: Service
metadata:
  name: astronova-frontend
  labels:
    {{- include "astronova.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "astronova.frontend.selectorLabels" . | nindent 4 }}
