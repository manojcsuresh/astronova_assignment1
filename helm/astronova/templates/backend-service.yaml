apiVersion: v1
kind: Service
metadata:
  name: astronova-backend
  labels:
    {{- include "astronova.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "astronova.backend.selectorLabels" . | nindent 4 }}
